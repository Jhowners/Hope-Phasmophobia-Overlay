<Window x:Class="Hophesmoverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hophesmoverlay"
        mc:Ignorable="d"
        Title="Hophesmoverlay" Height="750" Width="520"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <local:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <SolidColorBrush x:Key="GlassBg" Color="#66000000"/>
        <LinearGradientBrush x:Key="BorderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#9d00ff" Offset="0.0"/>
            <GradientStop Color="#8a0303" Offset="1.0"/>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#44000000"/>
            <Setter Property="Foreground" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="#666" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#88444444"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonCheckbox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#BBBBBB"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="18" Height="18" BorderBrush="#666" BorderThickness="1" CornerRadius="4" Background="#44000000" Margin="0,0,8,0">
                                <Path Data="M 2,9 L 6,13 L 14,4" Stroke="#9d00ff" StrokeThickness="2" Visibility="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}">
                                    <Path.Effect>
                                        <DropShadowEffect Color="#9d00ff" BlurRadius="6" ShadowDepth="0"/>
                                    </Path.Effect>
                                </Path>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border x:Name="MainJournal" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="12" Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.4"/>
            </Border.Effect>
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="HOPE PHASMOPHOBIA OVERLAY" FontFamily="Segoe UI Light" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#9d00ff" BlurRadius="15" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="[INS] MINI | [HOME] INTEL | [ALT+8] RESET" Foreground="#AAA" FontSize="10" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Button Content="☕ Support" HorizontalAlignment="Right" VerticalAlignment="Top" Background="#66332200" Foreground="#FFD700" Click="BtnDonate_Click"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="190"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="Transparent" CornerRadius="8" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Hidden">
                            <ItemsControl Name="GhostsListControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,4" CornerRadius="8" Height="60" Visibility="{Binding IsEliminated, Converter={StaticResource InverseBoolToVis}}" BorderThickness="2">
                                            <Border.BorderBrush>
                                                <DrawingBrush Stretch="Fill">
                                                    <DrawingBrush.Drawing>
                                                        <GeometryDrawing Brush="{Binding EvidenceGradient}">
                                                            <GeometryDrawing.Geometry>
                                                                <RectangleGeometry Rect="0,0,100,100"/>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                    </DrawingBrush.Drawing>
                                                </DrawingBrush>
                                            </Border.BorderBrush>
                                            <Border.Background>
                                                <SolidColorBrush Color="#66000000"/>
                                            </Border.Background>
                                            <Grid ClipToBounds="True">
                                                <TextBlock Text="{Binding Symbol}" FontSize="55" Foreground="{Binding PrimaryColorBrush}" Opacity="0.4" FontFamily="Segoe UI Symbol" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" RenderTransformOrigin="0.5,0.5">
                                                    <TextBlock.Effect>
                                                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="White" Opacity="0.3"/>
                                                    </TextBlock.Effect>
                                                    <TextBlock.RenderTransform>
                                                        <RotateTransform Angle="-5"/>
                                                    </TextBlock.RenderTransform>
                                                </TextBlock>
                                                <StackPanel VerticalAlignment="Center" Margin="15,0">
                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="ExtraBold" FontSize="16">
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding Tell}" Foreground="#DDD" FontSize="11" FontStyle="Italic" TextWrapping="Wrap" Margin="0,2,0,0">
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="EVIDENCE (ALT+1-7)" Foreground="#BBB" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                        <StackPanel>
                            <CheckBox x:Name="ChkEv1" Content="EMF Level 5" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv2" Content="Spirit Box" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv3" Content="Fingerprints" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv4" Content="Ghost Orb" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv5" Content="Ghost Writing" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv6" Content="Freezing" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                            <CheckBox x:Name="ChkEv7" Content="D.O.T.S." Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed"/>
                        </StackPanel>

                        <Separator Background="#555" Margin="0,10"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#66000000" BorderBrush="#444" BorderThickness="1" CornerRadius="6" Padding="5">
                                <StackPanel>
                                    <TextBlock Text="SMUDGE [F9]" Foreground="#AAA" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Name="TxtTimer" Text="READY" Foreground="White" FontSize="20" FontWeight="Light" HorizontalAlignment="Center" Margin="0,2"/>
                                    <TextBlock Name="TxtTimerStatus" Text="WAITING..." FontSize="9" Foreground="#777" HorizontalAlignment="Center"/>
                                    <ProgressBar Name="ProgressTimer" Height="3" Maximum="180" Value="180" Background="#33FFFFFF" BorderThickness="0" Foreground="#9d00ff"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#66000000" BorderBrush="#444" BorderThickness="1" CornerRadius="6" Padding="5">
                                <StackPanel>
                                    <TextBlock Text="HUNT CD [F7]" Foreground="#AAA" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Name="TxtHuntTimer" Text="READY" Foreground="White" FontSize="20" FontWeight="Light" HorizontalAlignment="Center" Margin="0,2"/>
                                    <TextBlock Text="SAFE?" FontSize="9" Foreground="#555" HorizontalAlignment="Center"/>
                                    <ProgressBar Name="ProgressHuntTimer" Height="3" Maximum="25" Value="25" Background="#33FFFFFF" BorderThickness="0" Foreground="#FF4444"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Margin="0,10,0,0" Background="#66000000" BorderBrush="#444" BorderThickness="1" CornerRadius="6" Padding="12">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="PACER" Foreground="#AAA" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="[F10]" Foreground="#777" FontSize="9" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <Button Content="RST (F11)" HorizontalAlignment="Right" Click="BtnResetPace_Click" FontSize="9"/>
                                    </StackPanel>
                                </Grid>
                                <ComboBox Name="CmbSpeed" SelectedIndex="2" Margin="0,5" Background="#88222222" Foreground="White" FontSize="11" BorderBrush="Transparent" SelectionChanged="CmbSpeed_SelectionChanged">
                                    <ComboBoxItem Content="50% Speed"/>
                                    <ComboBoxItem Content="75% Speed"/>
                                    <ComboBoxItem Content="100% (Default)"/>
                                    <ComboBoxItem Content="125% Speed"/>
                                    <ComboBoxItem Content="150% Speed"/>
                                </ComboBox>
                                <TextBlock Name="TxtBPM" Text="-- BPM" Foreground="White" FontSize="28" FontWeight="Light" HorizontalAlignment="Center" Margin="0,2">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Name="TxtGhostSpeedGuess" Text="Select Speed &amp; Tap" FontSize="10" Foreground="#777" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock Name="TxtPacerStatus" Text="FILTER: NONE" FontSize="9" Foreground="#666" HorizontalAlignment="Center" Margin="0,5,0,0" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border x:Name="MiniHud" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,50,0,0" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="8" Padding="15">
            <StackPanel>
                <TextBlock Text="SMUDGE" FontSize="9" Foreground="#999" HorizontalAlignment="Center"/>
                <TextBlock Name="TxtMiniTimer" Text="03:00" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Name="TxtMiniStatus" Text="SAFE" FontSize="10" FontWeight="Bold" Foreground="LightGreen" HorizontalAlignment="Center"/>

                <TextBlock Text="HUNT CD" FontSize="9" Foreground="#999" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Name="TxtMiniHuntTimer" Text="00:25" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border x:Name="IntelHud" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,50,0,0" Width="260" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#20FFFFFF" Padding="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="SMUDGE" FontSize="9" Foreground="#88AAFF" HorizontalAlignment="Center"/>
                            <TextBlock Name="TxtIntelTimer" Text="03:00" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Name="TxtIntelStatus" Text="SAFE" FontSize="9" Foreground="LightGreen" HorizontalAlignment="Center"/>

                            <TextBlock Text="HUNT CD" FontSize="9" Foreground="#FF8888" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            <TextBlock Name="TxtIntelHuntTimer" Text="00:25" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <Grid Margin="0,0,0,2" HorizontalAlignment="Right">
                                <TextBlock Text="SPEED" FontSize="9" Foreground="#88AAFF" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <Button Content="CLR" FontSize="8" Padding="4,1" Margin="0" HorizontalAlignment="Right" Click="BtnResetPace_Click"/>
                            </Grid>
                            <TextBlock Name="TxtIntelBPM" Text="-- BPM" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Right"/>
                            <TextBlock Name="TxtIntelSpeedGuess" Text="Waiting" FontSize="9" Foreground="#BBB" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Padding="12,8" BorderBrush="#333" BorderThickness="0,1">
                    <StackPanel>
                        <TextBlock Text="MARKED EVIDENCE" FontSize="9" Foreground="#AAA"/>
                        <TextBlock Name="TxtIntelEvidence" Text="NONE" FontSize="11" Foreground="Yellow" TextWrapping="Wrap" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
                <Border Grid.Row="2" Padding="12" MaxHeight="350">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <StackPanel>
                            <TextBlock Text="POSSIBLE TARGETS" FontSize="9" Foreground="#AAA" Margin="0,0,0,5"/>
                            <ItemsControl Name="GhostsIntelControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,2" CornerRadius="4" Padding="5" Background="#44000000" BorderThickness="1" Visibility="{Binding IsEliminated, Converter={StaticResource InverseBoolToVis}}">
                                            <Border.BorderBrush>
                                                <DrawingBrush Stretch="Fill">
                                                    <DrawingBrush.Drawing>
                                                        <GeometryDrawing Brush="{Binding EvidenceGradient}">
                                                            <GeometryDrawing.Geometry>
                                                                <RectangleGeometry Rect="0,0,100,100"/>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                    </DrawingBrush.Drawing>
                                                </DrawingBrush>
                                            </Border.BorderBrush>
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="8" Height="8" Fill="{Binding PrimaryColorBrush}" Margin="0,0,8,0" VerticalAlignment="Center">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="{Binding PrimaryColor}" BlurRadius="5" ShadowDepth="0" Opacity="0.8"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" FontWeight="Bold" VerticalAlignment="Center">
                                                    <TextBlock.Effect>
                                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1"/>
                                                    </TextBlock.Effect>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
<Window x:Class="Hophesmoverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hophesmoverlay"
        mc:Ignorable="d"
        Title="Hophesmoverlay" Height="750" Width="520"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="CanResizeWithGrip"
        MouseDown="Window_MouseDown"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed">

    <Window.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <local:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:EvidenceColorConverter x:Key="EvColorConv"/>

        <SolidColorBrush x:Key="GlassBg" Color="#77000000"/>
        <LinearGradientBrush x:Key="BorderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#9d00ff" Offset="0.0"/>
            <GradientStop Color="#8a0303" Offset="1.0"/>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#44000000"/>
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="#777" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#88444444"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BehaviorToggle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Border" Background="#99000000" BorderBrush="#888" BorderThickness="1" CornerRadius="4" Padding="10,6">
                            <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" 
                                       FontSize="12" FontWeight="SemiBold" Foreground="White" 
                                       TextAlignment="Center" TextWrapping="Wrap" MaxWidth="110"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#CC228822"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#00FF00"/>
                                <Setter TargetName="Txt" Property="Foreground" Value="White"/>
                                <Setter TargetName="Txt" Property="FontWeight" Value="ExtraBold"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF00" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF444444"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonCheckbox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="IsThreeState" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="18" Height="18" BorderBrush="#999" BorderThickness="1" CornerRadius="4" Background="#44000000" Margin="0,0,8,0">
                                <Grid>
                                    <Path Name="CheckMark" Data="M 2,9 L 6,13 L 14,4" Stroke="#9d00ff" StrokeThickness="2" Visibility="Collapsed"/>
                                    <Path Name="XMark" Data="M 3,3 L 15,15 M 15,3 L 3,15" Stroke="#FF4444" StrokeThickness="2" Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="XMark" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="#FF8888"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border x:Name="MainJournal" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="12" Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="2"/>
            </Border.Effect>

            <Border.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Language / Idioma">
                        <MenuItem Header="English" Click="MenuEn_Click"/>
                        <MenuItem Header="Português (Brasil)" Click="MenuPt_Click"/>
                        <MenuItem Header="日本語" Click="MenuJp_Click"/>
                        <MenuItem Header="繁體中文" Click="MenuCht_Click"/>
                        <MenuItem Header="Deutsch" Click="MenuDe_Click"/>
                        <MenuItem Header="Русский" Click="MenuRu_Click"/>
                        <MenuItem Header="Español" Click="MenuEs_Click"/>
                        <MenuItem Header="Nederlands" Click="MenuNl_Click"/>
                        <MenuItem Header="Українська" Click="MenuUk_Click"/>
                        <MenuItem Header="Français" Click="MenuFr_Click"/>
                    </MenuItem>
                    <Separator/>
                    <MenuItem x:Name="MenuDiscord" Header="Discord RPC" IsCheckable="True" IsChecked="True" Click="MenuDiscord_Click"/>
                    <Separator/>
                    <MenuItem Header="Exit / Sair" Click="MenuExit_Click"/>
                </ContextMenu>
            </Border.ContextMenu>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="HOPE PHASMOPHOBIA OVERLAY" FontFamily="Segoe UI" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#9d00ff" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock x:Name="LblControls" Text="[PSBREAK] MINI | [HOME] INTEL | [ALT+8] RESET" Foreground="#CCC" FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Button Content="☕ Support" HorizontalAlignment="Right" VerticalAlignment="Top" Background="#66332200" Foreground="#FFD700" Click="BtnDonate_Click" BorderBrush="#995500"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="190"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="Transparent" CornerRadius="8" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Hidden">
                            <ItemsControl Name="GhostsListControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,4" CornerRadius="8" MinHeight="68" Visibility="{Binding IsEliminated, Converter={StaticResource InverseBoolToVis}}" BorderThickness="2">
                                            <Border.BorderBrush>
                                                <DrawingBrush Stretch="Fill">
                                                    <DrawingBrush.Drawing>
                                                        <GeometryDrawing Brush="{Binding EvidenceGradient}">
                                                            <GeometryDrawing.Geometry>
                                                                <RectangleGeometry Rect="0,0,100,100"/>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                    </DrawingBrush.Drawing>
                                                </DrawingBrush>
                                            </Border.BorderBrush>
                                            <Border.Background>
                                                <SolidColorBrush Color="#99050505"/>
                                            </Border.Background>
                                            <Grid ClipToBounds="True">
                                                <TextBlock Text="{Binding Symbol}" FontSize="55" Foreground="{Binding PrimaryColorBrush}" Opacity="0.25" FontFamily="Segoe UI Symbol" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" RenderTransformOrigin="0.5,0.5">
                                                    <TextBlock.RenderTransform>
                                                        <RotateTransform Angle="-5"/>
                                                    </TextBlock.RenderTransform>
                                                </TextBlock>

                                                <StackPanel VerticalAlignment="Center" Margin="15,4">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="17">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                                                            </TextBlock.Effect>
                                                        </TextBlock>
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                            <Border HorizontalAlignment="Left" Margin="10,2,0,0" Background="#BB000000" CornerRadius="3" Padding="6,1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding HuntThreshold}" Foreground="#FF6666" FontSize="10" FontWeight="ExtraBold" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <Border Margin="6,2,0,0" Background="#2200FFFF" CornerRadius="3" Padding="6,1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding SpeedInfo}" Foreground="#00F0FF" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </StackPanel>
                                                    </Grid>

                                                    <ItemsControl ItemsSource="{Binding Evidences}" Margin="0,4,0,2">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <WrapPanel Orientation="Horizontal"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border BorderBrush="{Binding Converter={StaticResource EvColorConv}}" BorderThickness="1" CornerRadius="3" Margin="0,0,4,4" Padding="5,1" Background="#44000000">
                                                                    <TextBlock Text="{Binding}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding Converter={StaticResource EvColorConv}}"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <TextBlock Text="{Binding GuaranteedText}" Visibility="{Binding HasGuaranteed, Converter={StaticResource BoolToVis}}" Foreground="#FFD700" FontSize="10" FontWeight="Bold" Margin="0,0,0,0" FontStyle="Italic"/>
                                                    <TextBlock Text="{Binding Tell}" Foreground="#EEEEEE" FontSize="11" FontStyle="Normal" TextWrapping="Wrap" Margin="0,2,0,0" MaxWidth="330" LineHeight="14"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                    
                    <StackPanel Grid.Column="2">

                            <ToggleButton Name="BtnModeSwitch" Height="26" FontSize="11" FontWeight="Bold" Margin="0,0,0,10" Background="#44222222" Foreground="#00DDFF">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Content" Value="SWITCH TO: 0 EVIDENCE MODE"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="TBorder" Background="{TemplateBinding Background}" BorderBrush="#00DDFF" BorderThickness="1" CornerRadius="4">
                                                        <TextBlock Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Content" Value="BACK TO: STANDARD MODE"/>
                                                            <Setter TargetName="TBorder" Property="Background" Value="#44AA0000"/>
                                                            <Setter TargetName="TBorder" Property="BorderBrush" Value="#FF4444"/>
                                                            <Setter Property="Foreground" Value="#FF9999"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>

                            <StackPanel Name="PanelEvidence" Visibility="{Binding ElementName=BtnModeSwitch, Path=IsChecked, Converter={StaticResource InverseBoolToVis}}">
                                <TextBlock x:Name="LblEvidence" Text="EVIDENCE (ALT+1-7)" Foreground="#CCC" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                                <StackPanel>
                                    <CheckBox x:Name="ChkEv1" Content="EMF Level 5" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv2" Content="D.O.T.S." Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv3" Content="Ultraviolet" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv4" Content="Freezing" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv5" Content="Ghost Orb" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv6" Content="Ghost Writing" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <CheckBox x:Name="ChkEv7" Content="Spirit Box" Style="{StaticResource NeonCheckbox}" Checked="Evidence_Changed" Unchecked="Evidence_Changed" Indeterminate="Evidence_Changed"/>
                                    <TextBlock x:Name="TxtSystemMessage" Text="" Foreground="Orange" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,5,5,0"/>
                                </StackPanel>
                            </StackPanel>

                            <ScrollViewer Name="PanelZeroEv" Visibility="{Binding ElementName=BtnModeSwitch, Path=IsChecked, Converter={StaticResource BoolToVis}}" VerticalScrollBarVisibility="Hidden" MaxHeight="350">
                                <StackPanel>
                                <TextBlock Text="GENDER" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                <WrapPanel Margin="0,0,0,8">
                                    <CheckBox Content="Male Ghost" Style="{StaticResource BehaviorToggle}" Tag="MaleGhost" Click="Behavior_Click"/>
                                    <CheckBox Content="Female Ghost" Style="{StaticResource BehaviorToggle}" Tag="FemaleGhost" Click="Behavior_Click"/>
                                </WrapPanel>
                                <TextBlock Text="HUNT VISUALS" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                    <WrapPanel Margin="0,0,0,8">
                                        <CheckBox Content="Fast Blink" Style="{StaticResource BehaviorToggle}" Tag="PhantomBlink" Click="Behavior_Click"/>
                                        <CheckBox Content="Slow Blink" Style="{StaticResource BehaviorToggle}" Tag="OniBlink" Click="Behavior_Click"/>
                                        <CheckBox Content="Shapeshift" Style="{StaticResource BehaviorToggle}" Tag="ObakeShape" Click="Behavior_Click"/>
                                        <CheckBox Content="Freezing Breath" Style="{StaticResource BehaviorToggle}" Tag="HantuBreath" Click="Behavior_Click"/>
                                        <CheckBox Content="Wallhack" Style="{StaticResource BehaviorToggle}" Tag="DeogenWallhack" Click="Behavior_Click"/>
                                    </WrapPanel>

                                    <TextBlock Text="SPEED &amp; AUDIO" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                    <WrapPanel Margin="0,0,0,8">
                                        <CheckBox Content="Quiet Steps" Style="{StaticResource BehaviorToggle}" Tag="MylingQuiet" Click="Behavior_Click"/>
                                        <CheckBox Content="Deaf/Blind" Style="{StaticResource BehaviorToggle}" Tag="YokaiDeaf" Click="Behavior_Click"/>
                                        <CheckBox Content="Defensive Speed" Style="{StaticResource BehaviorToggle}" Tag="GalluVariable" Click="Behavior_Click"/>
                                        <CheckBox Content="Variable Speed" Style="{StaticResource BehaviorToggle}" Tag="ObamboVariable" Click="Behavior_Click"/>
                                        <CheckBox Content="Fast Near Electronics" Style="{StaticResource BehaviorToggle}" Tag="RaijuElectronics" Click="Behavior_Click"/>
                                    </WrapPanel>

                                    <TextBlock Text="PHYSICS &amp; INTERACTIONS" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                    <WrapPanel Margin="0,0,0,8">
                                        <CheckBox Content="Stepped in Salt" Style="{StaticResource BehaviorToggle}" Tag="SaltStep" Click="Behavior_Click"/>
                                        <CheckBox Content="Airball Event" Style="{StaticResource BehaviorToggle}" Tag="Airball" Click="Behavior_Click"/>
                                        <CheckBox Content="Breaker OFF" Style="{StaticResource BehaviorToggle}" Tag="BreakerOff" Click="Behavior_Click"/>
                                        <CheckBox Content="Full Door Slam" Style="{StaticResource BehaviorToggle}" Tag="YureiSlam" Click="Behavior_Click"/>
                                        <CheckBox Content="Multi-Throw" Style="{StaticResource BehaviorToggle}" Tag="PoltyThrow" Click="Behavior_Click"/>
                                        <CheckBox Content="Light Switch Trap" Style="{StaticResource BehaviorToggle}" Tag="MareLightTrap" Click="Behavior_Click"/>
                                        <CheckBox Content="Disappeared on Photo" Style="{StaticResource BehaviorToggle}" Tag="PhantomPhoto" Click="Behavior_Click"/>
                                    </WrapPanel>

                                    <TextBlock Text="TESTS" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                    <WrapPanel Margin="0,0,0,8">
                                        <CheckBox Content="Banshee Scream" Style="{StaticResource BehaviorToggle}" Tag="BansheeScream" Click="Behavior_Click"/>
                                        <CheckBox Content="Candle Hunt (3rd)" Style="{StaticResource BehaviorToggle}" Tag="OnryoCandle" Click="Behavior_Click"/>
                                        <CheckBox Content="Mimic Orbs" Style="{StaticResource BehaviorToggle}" Tag="MimicOrbs" Click="Behavior_Click"/>
                                    </WrapPanel>
                                <TextBlock Text="BEHAVIOR &amp; TIMING" Foreground="#00DDFF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                <WrapPanel Margin="0,0,0,8">
                                    <CheckBox Content="Shy / Hard to Hunt" Style="{StaticResource BehaviorToggle}" Tag="ShadeShy" Click="Behavior_Click"/>
                                    <CheckBox Content="Never Roams" Style="{StaticResource BehaviorToggle}" Tag="GoryoRoam" Click="Behavior_Click"/>
                                    <CheckBox Content="Aging / Slows Down" Style="{StaticResource BehaviorToggle}" Tag="ThayeAging" Click="Behavior_Click"/>
                                    <CheckBox Content="Curse / Fast Drain" Style="{StaticResource BehaviorToggle}" Tag="MoroiCurse" Click="Behavior_Click"/>
                                    <CheckBox Content="Twin Interaction" Style="{StaticResource BehaviorToggle}" Tag="TwinsInteraction" Click="Behavior_Click"/>

                                    <CheckBox Content="Hunted &lt; 60s" Style="{StaticResource BehaviorToggle}" Tag="DemonSmudge" Click="Behavior_Click"/>
                                    <CheckBox Content="Safe > 180s" Style="{StaticResource BehaviorToggle}" Tag="SpiritSmudge" Click="Behavior_Click"/>
                                </WrapPanel>
                                <TextBlock Text="PRO CHECKS &amp; HIDDEN" Foreground="#FF00FF" FontSize="10" FontWeight="Bold" Margin="0,5,0,2"/>
                                <WrapPanel Margin="0,0,0,8">
                                    <CheckBox Content="Silent Footstep" Style="{StaticResource BehaviorToggle}" Tag="MylingFlicker" Click="Behavior_Click"/>
                                    <CheckBox Content="Long Smudge Blind" Style="{StaticResource BehaviorToggle}" Tag="MoroiBlind" Click="Behavior_Click"/>
                                    <CheckBox Content="Teleported to Me" Style="{StaticResource BehaviorToggle}" Tag="WraithTeleport" Click="Behavior_Click"/>
                                    <CheckBox Content="Ability at Breaker" Style="{StaticResource BehaviorToggle}" Tag="JinnBreakerAbility" Click="Behavior_Click"/>

                                    <CheckBox Content="Stepped Salt 2x (Not Gallu)" Style="{StaticResource BehaviorToggle}" Tag="GalluDoubleSalt" Click="Behavior_Click"/>
                                    <CheckBox Content="Turned Light ON (Not Mare)" Style="{StaticResource BehaviorToggle}" Tag="LightOn" Click="Behavior_Click"/>
                                    <CheckBox Content="Turned Breaker ON (Not Hantu)" Style="{StaticResource BehaviorToggle}" Tag="BreakerOn" Click="Behavior_Click"/>
                                    <CheckBox Content="Changed Room (Not Goryo)" Style="{StaticResource BehaviorToggle}" Tag="GoryoRoam" Click="Behavior_Click"/>
                                    <CheckBox Content="Airball Event (Not Oni)" Style="{StaticResource BehaviorToggle}" Tag="OniAirball" Click="Behavior_Click"/>
                                    <CheckBox Content="Sang/Hiss Event (Not Shade)" Style="{StaticResource BehaviorToggle}" Tag="ShadeSinging" Click="Behavior_Click"/>
                                </WrapPanel>
                            </StackPanel>
                            </ScrollViewer>

                            <Separator Background="#666" Margin="0,12"/>

                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#66000000" BorderBrush="#555" BorderThickness="1" CornerRadius="6" Padding="6">
                                <StackPanel>
                                    <TextBlock x:Name="LblSmudge" Text="SMUDGE [F9]" Foreground="#CCC" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Name="TxtTimer" Text="READY" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,3"/>
                                    <TextBlock Name="TxtTimerStatus" Text="WAITING..." FontSize="10" Foreground="#999" HorizontalAlignment="Center"/>
                                    <ProgressBar Name="ProgressTimer" Height="4" Maximum="180" Value="180" Background="#33FFFFFF" BorderThickness="0" Foreground="#9d00ff" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="2" Background="#66000000" BorderBrush="#555" BorderThickness="1" CornerRadius="6" Padding="6">
                                <StackPanel>
                                    <TextBlock x:Name="LblHunt" Text="CRUCIFIX [F7]" Foreground="#CCC" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Name="TxtHuntTimer" Text="READY" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,3"/>
                                    <TextBlock Name="TxtHuntTimerStatus" Text="WAITING..." FontSize="10" Foreground="#999" HorizontalAlignment="Center"/>
                                    <ProgressBar Name="ProgressHuntTimer" Height="4" Maximum="25" Value="25" Background="#33FFFFFF" BorderThickness="0" Foreground="#FF4444" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Margin="0,12,0,0" Background="#44220000" BorderBrush="#FF4444" BorderThickness="1" CornerRadius="6" Padding="8">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="HUNT [F1]" Foreground="#FF8888" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <ComboBox Grid.Column="1" Name="CmbMapSize" Width="85" FontSize="9" Padding="2" SelectedIndex="0" Background="#99000000" Foreground="Black" BorderThickness="0">
                                        <ComboBoxItem Content="Small (30s)"/>
                                        <ComboBoxItem Content="Medium (50s)"/>
                                        <ComboBoxItem Content="Large (60s)"/>
                                    </ComboBox>
                                </Grid>
                                <TextBlock Name="TxtActiveHunt" Text="-- / 30s" Foreground="#FF4444" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,0">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,12,0,0" Background="#66000000" BorderBrush="#555" BorderThickness="1" CornerRadius="6" Padding="12">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <TextBlock x:Name="LblSpeed" Text="SPEED (m/s)" Foreground="#CCC" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="[F10]" Foreground="#888" FontSize="10" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <Button Content="RST (F11)" HorizontalAlignment="Right" Click="BtnResetPace_Click" FontSize="9"/>
                                    </StackPanel>
                                </Grid>
                                <ComboBox Name="CmbSpeed" SelectedIndex="2" Margin="0,5" Background="#99222222" Foreground="Black" FontSize="11" BorderBrush="#555" SelectionChanged="CmbSpeed_SelectionChanged" Padding="4">
                                    <ComboBoxItem Content="50% Speed"/>
                                    <ComboBoxItem Content="75% Speed"/>
                                    <ComboBoxItem Content="100% (Default)"/>
                                    <ComboBoxItem Content="Blood Moon (+15%)" Foreground="#FF5555" FontWeight="Bold"/>
                                    <ComboBoxItem Content="125% Speed"/>
                                    <ComboBoxItem Content="150% Speed"/>
                                </ComboBox>
                                <TextBlock Name="TxtBPM" Text="-- m/s" Foreground="White" FontSize="34" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="2" Opacity="0.8"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Name="TxtGhostSpeedGuess" Text="Select Speed &amp; Tap" FontSize="11" Foreground="#DDD" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock Name="TxtPacerStatus" Text="FILTER: NONE" FontSize="10" Foreground="#888" HorizontalAlignment="Center" Margin="0,8,0,0" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border x:Name="MiniHud" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,50,0,0" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="8" Padding="15">
            <StackPanel>
                <TextBlock x:Name="LblMiniSmudge" Text="SMUDGE" FontSize="10" Foreground="#BBB" HorizontalAlignment="Center"/>
                <TextBlock Name="TxtMiniTimer" Text="03:00" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Name="TxtMiniStatus" Text="SAFE" FontSize="11" FontWeight="Bold" Foreground="LightGreen" HorizontalAlignment="Center"/>
                <TextBlock x:Name="LblMiniHunt" Text="HUNT CD" FontSize="10" Foreground="#BBB" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <TextBlock Name="TxtMiniHuntTimer" Text="00:25" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>
        </Border>

        <Border x:Name="IntelHud" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,50,0,0" Width="260" Background="{StaticResource GlassBg}" BorderBrush="{StaticResource BorderGradient}" BorderThickness="2" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#33FFFFFF" Padding="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock x:Name="LblIntelSmudge" Text="SMUDGE" FontSize="10" Foreground="#AAEEFF" HorizontalAlignment="Center"/>
                            <TextBlock Name="TxtIntelTimer" Text="03:00" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Name="TxtIntelStatus" Text="SAFE" FontSize="10" Foreground="LightGreen" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="LblIntelHunt" Text="HUNT CD" FontSize="10" Foreground="#FFAAAA" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            <TextBlock Name="TxtIntelHuntTimer" Text="00:25" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <Grid Margin="0,0,0,4" HorizontalAlignment="Right">
                                <TextBlock x:Name="LblIntelSpeed" Text="SPEED" FontSize="10" Foreground="#AAEEFF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <Button Content="CLR" FontSize="9" Padding="4,2" Margin="0" HorizontalAlignment="Right" Click="BtnResetPace_Click"/>
                            </Grid>
                            <TextBlock Name="TxtIntelBPM" Text="-- m/s" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Right"/>
                            <TextBlock Name="TxtIntelSpeedGuess" Text="Waiting" FontSize="10" Foreground="#DDD" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Padding="12,8" BorderBrush="#444" BorderThickness="0,1">
                    <StackPanel>
                        <TextBlock x:Name="LblIntelEvidence" Text="MARKED EVIDENCE" FontSize="10" Foreground="#CCC"/>
                        <TextBlock Name="TxtIntelFound" Text="NONE" FontSize="12" Foreground="Yellow" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,2,0,0"/>
                        <TextBlock Name="TxtIntelRuledOut" Text="" FontSize="11" Foreground="#FF6666" TextWrapping="Wrap" FontWeight="SemiBold" Visibility="Collapsed" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                <Border Grid.Row="2" Padding="12" MaxHeight="350">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <StackPanel>
                            <TextBlock x:Name="LblIntelTargets" Text="POSSIBLE TARGETS" FontSize="10" Foreground="#CCC" Margin="0,0,0,8"/>
                            <ItemsControl Name="GhostsIntelControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,3" CornerRadius="5" Padding="6" Background="#55000000" BorderBrush="#666" BorderThickness="1" Visibility="{Binding IsEliminated, Converter={StaticResource InverseBoolToVis}}">
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="10" Height="10" Fill="{Binding PrimaryColorBrush}" Margin="0,0,10,0" VerticalAlignment="Center">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="{Binding PrimaryColor}" BlurRadius="6" Opacity="0.8" ShadowDepth="0"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>